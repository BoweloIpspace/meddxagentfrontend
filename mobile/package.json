{
  "name": "meddxagent-mobile",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "android:init": "cap add android",
    "android:sync": "npm --prefix .. run build && cap sync android",
    "android:open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "8.4.0",
    "@capacitor/core": "8.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.4.0"
  }
}
