{
  "name": "meddxagent-mobile",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "native:prepare": "node prepare-native.mjs",
    "android:init": "cap add android",
    "android:sync": "npm --prefix .. run build && npm run native:prepare && cap sync android",
    "android:open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "8.4.0",
    "@capacitor/core": "8.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.4.0"
  }
}
